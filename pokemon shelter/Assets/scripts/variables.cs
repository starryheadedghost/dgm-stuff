using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class variables : MonoBehaviour
{
    //[acces modifier] [data-type/type] [idendifier/name] [access operator =] [value]

    public string NPCname = "sir Nor MalMan";

// Nor MalMan likes bread :)
    public int NPCbread = 10;

//illegitamite children
    private float NPCilchildren = 3.4f;
    
    public char NPCfavletter = 'm';

    public bool NPCgood = false; 

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
